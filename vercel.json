{
  "buildCommand": "npm run build && npx prisma migrate deploy && npx prisma db seed",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "env": [
    "DATABASE_URL",
    "JWT_SECRET"
  ],
  "framework": "nextjs"
}
